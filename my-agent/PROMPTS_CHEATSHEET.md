# 🤖 AI Промпт шпаргалка

## Шаблоны промптов для каждого агента

### 🎯 Orchestrator

**Начало проекта:**
```
Начинаем проект Cube World.
Текущая фаза: 0 (Подготовка).
Проверь готовность и скажи, что делать дальше.
```

**Проверка прогресса:**
```
Какая сейчас текущая фаза проекта?
Какие задачи выполнены, что осталось?
```

**Переход к следующей фазе:**
```
Фаза [X] завершена и протестирована.
Готовы переходить к Фазе [X+1]?
```

---

### 🏗️ VoxelEngine

**Фаза 1.1: Базовый чанк**
```
Создай C# скрипт Chunk.cs для Unity, который генерирует
меш воксельного чанка 16x16x16. Каждый блок — куб.
Генерируй только видимые грани (не рендери грани между
соседними солидными блоками). Используй текстурный атлас.
```

**Фаза 1.2: Greedy Meshing**
```
Добавь Greedy Meshing к моему воксельному чанку.
Объединяй соседние одинаковые грани в один большой полигон.
Покажи до/после количество вершин.
```

**Фаза 1.3: ChunkManager**
```
Создай ChunkManager, который загружает чанки в радиусе
8 чанков от игрока и выгружает дальние. Используй Unity
Job System для генерации мешей в фоне. Убедись, что между
чанками нет швов.
```

**Фаза 1.4: Процедурная генерация**
```
Добавь процедурную генерацию ландшафта используя
многооктавный Perlin Noise. Высота от 0 до 128 блоков.
Добавь пещеры через 3D шум. Блоки: трава сверху, земля
под ней, камень глубже.
```

**Фаза 1.5: Стиль Cube World**
```
Модифицируй генератор мешей, чтобы вместо острых кубов
генерировать слегка скруглённые воксели в стиле Cube World.
Можно использовать Marching Cubes или добавить фаски к
граням блоков.
```

---

### 🎮 PlayerController

**Фаза 2.1: Контроллер**
```
Создай контроллер персонажа от третьего лица для Unity.
WASD движение, Space — прыжок, Shift — бег. Используй
CharacterController. Персонаж должен корректно ходить по
воксельному ландшафту.
```

**Фаза 2.2: Камера**
```
Настрой Cinemachine FreeLook камеру от третьего лица.
Вращение правой кнопкой мыши, зум колёсиком, камера не
проходит сквозь стены. Расстояние по умолчанию — 10 юнитов.
```

---

### 🎨 VisualStyle

**Фаза 3.1: День/ночь**
```
Создай систему цикла дня/ночи для Unity. Directional light
вращается за 10 минут реального времени. Меняй цвет skybox,
ambient light и fog в зависимости от времени суток. Ночью
темно, на рассвете оранжевый свет.
```

**Фаза 3.2: Вода**
```
Добавь воду в воксельный мир. Уровень воды на высоте Y=40.
Создай шейдер для воды: полупрозрачность, анимация волн,
отражения. Вода заполняет впадины, образуя озёра.
```

**Фаза 3.3: Растительность**
```
Добавь процедурные воксельные деревья. 3-4 типа деревьев
(дуб, ель, пальма, берёза). Деревья случайно размещаются
при генерации чанка. Добавь траву как billboard-спрайты
на верхних блоках.
```

**Фаза 3.4: Биомы**
```
Создай систему биомов на основе карт температуры и влажности
(Perlin Noise). 5 биомов: луга, лес, пустыня, снежные горы,
океан. Плавные переходы между биомами. Каждый биом имеет
свои блоки и растительность.
```

---

### ⚔️ CombatSystem

**Фаза 4.1: Система оружия**
```
Создай систему оружия для action RPG. ScriptableObject
WeaponData (имя, урон, скорость атаки, дальность, тип).
Скрипт WeaponController: левая кнопка мыши — атака,
определение попадания через Physics.OverlapSphere.
Простая анимация замаха.
```

**Фаза 4.2: Комбо**
```
Добавь комбо-систему: 3 удара в цепочке, каждый следующий
сильнее. Окно для ввода следующего удара — 0.5 сек после
предыдущего. Если не нажал — комбо сбрасывается. Добавь
Camera Shake при попадании.
```

**Фаза 4.3: Уклонение**
```
Добавь dodge roll по нажатию Space/Shift. Во время dodge
roll игрок неуязвим (iframe). Анимация переката. Добавь
stamina систему: каждый dodge тратит stamina, который
восстанавливается со временем.
```

**Фаза 4.4: Таргетинг**
```
Создай lock-on систему: Tab — зафиксировать ближайшего
врага. Камера автоматически поворачивается к цели.
Индикатор над головой таргета. Tab снова — сменить цель.
```

---

### 🧟 EnemyAI

**Фаза 5.1: Базовый AI**
```
Создай базовый AI для врагов через FSM (Finite State Machine).
Состояния: Idle, Patrol, Chase, Attack, Flee. Detection radius
через OnTriggerEnter. Враг патрулирует, при обнаружении
игрока переходит в Chase, затем Attack.
```

**Фаза 5.2: Pathfinding**
```
Реализуй A* pathfinding для воксельного мира. Враг должен
обходить препятствия, прыгать через 1-блочные барьеры.
Оптимизируй через path caching. Визуализируй путь через
Debug.DrawLine.
```

**Фаза 5.3: Типы врагов**
```
Создай 4 типа врагов: Melee (ближний бой), Ranged (стреляет),
Flying (летает), Tank (медленный, много HP). У каждого
свой EnemyData ScriptableObject с характеристиками.
Разное поведение в бою.
```

**Фаза 5.4: Боссы**
```
Создай босса с многофазной системой. При 100-75% HP — фаза 1,
при 75-50% — фаза 2 (новые атаки), при 50-0% — берсерк режим.
Boss Health Bar UI сверху экрана. Арена для босса.
```

---

### 📊 RPGSystem

**Фаза 6.1: Уровни**
```
Создай систему уровней. PlayerStats скрипт с характеристиками:
STR, DEX, INT, VIT. При получении XP заполняется прогресс-бар.
При левелапе: +5 очков характеристик для распределения.
UI для распределения очков.
```

**Фаза 6.2: Инвентарь**
```
Создай grid-based инвентарь (как в Diablo). 10x5 слотов.
Drag & Drop система через IBeginDragHandler, IDragHandler,
IEndDragHandler. ItemData ScriptableObject. Стакаемые
предметы (стаки до 99). UI с сеткой слотов.
```

**Фаза 6.3: Крафтинг**
```
Создай систему крафтинга. CraftingRecipe ScriptableObject:
входные ресурсы, выходной предмет. UI верстака: слева
рецепты, справа требуемые ресурсы. Кнопка "Craft".
Проверка наличия ресурсов перед крафтом.
```

---

### 🗺️ WorldContent

**Фаза 7.1: Данжены**
```
Создай процедурную генерацию данженов. Алгоритм: случайные
комнаты + коридоры между ними. Ловушки, сундуки, враги.
Boss room в конце (самая большая комната). Вход через
портал в мире.
```

**Фаза 7.2: Деревни**
```
Создай процедурную генерацию деревень. Voronoi диаграммы
для планировки участков. На участках: дома, магазины,
колодец, кузница. NPC ходят по деревне. Trader NPC
с магазином предметов.
```

**Фаза 7.3: Квесты**
```
Создай систему квестов. ScriptableObject QuestData:
описание, тип (kill/fetch/explore/deliver), цель, награды.
QuestManager отслеживает прогресс. UI: квест-лог по клавише
J, текущий квест отображается справа. Процедурная генерация:
случайная комбинация типа + цели + награды.
```

---

### ✨ Polish

**Фаза 9.1: Главное меню**
```
Создай главное меню с кнопками: Новая игра, Загрузить,
Настройки, Выход. Новая игра: выбор класса (4 варианта),
имя персонажа, кастомизация внешности (цвета). Красивый
фон, музыка. Анимации кнопок через DOTween.
```

**Фаза 9.2: Сохранения**
```
Создай систему сохранения. Сериализуй в JSON: позиция
игрока, уровень, инвентарь, активные квесты. Изменённые
чанки сохраняй отдельно (только блоки, которые игрок
изменил). Автосохранение каждые 5 минут. 3 слота сохранения.
```

**Фаза 9.3: Звук**
```
Создай AudioManager (синглтон). Фоновая музыка зависит от
биома и состояния (exploration/combat). Плавный crossfade
между треками. Звуковые эффекты через Audio Source Pool
(чтобы не создавать новые объекты). 3D звук для врагов
и окружения.
```

---

## 💡 Советы по формулировке

### ✅ Хороший промпт
- Конкретная задача
- Указан контекст (что уже есть)
- Описан ожидаемый результат
- Технические детали (классы, методы)

### ❌ Плохой промпт
- Слишком общий ("сделай игру")
- Без контекста
- Много задач в одном
- Без конкретики

---

*Используйте эти шаблоны для максимальной эффективности вайбкодинга!*