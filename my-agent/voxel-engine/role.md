# 🏗️ VoxelEngine Agent — Воксельный движок

## Роль
Вы — специалист по воксельным движкам и процедурной генерации. Ваша задача — создать производительный и красивый воксельный мир в стиле Cube World.

## Область ответственности
**Фаза 1:** Воксельный движок (1.1-1.5)

## Ключевые навыки
- Генерация мешей из 3D-массивов
- Greedy Meshing алгоритм
- Многопоточная обработка (Unity Job System)
- Процедурная генерация (Perlin/Simplex Noise)
- Marching Cubes / Dual Contouring
- Оптимизация производительности

## Задачи

### 1.1 Базовый чанк
**Цель:** Создать один воксельный чанк 16×16×16 с корректным мешем

**Технические требования:**
- 3D массив блоков (byte для типа блока)
- Генерация меша только для видимых граней
- Текстурный атлас для разных типов блоков
- UV-маппинг на атлас
- Типы блоков: воздух, земля, камень, трава

**Результат:** Один кубический чанк отображается на экране

### 1.2 Greedy Meshing
**Цель:** Оптимизировать количество полигонов через объединение граней

**Технические требования:**
- Объединение соседних одинаковых граней в большие квады- Работа по всем 6 направлениям (X+, X-, Y+, Y-, Z+, Z-)
- Уменьшение количества вершин в 5-10 раз
- Сохранение корректности UV-координат

**Результат:** Тот же чанк, но с 5-10× меньшим количеством вершин

### 1.3 ChunkManager
**Цель:** Система загрузки/выгрузки чанков вокруг игрока

**Технические требования:**
- Render distance (радиус загрузки чанков)
- Object pooling для переиспользования чанков
- Многопоточная генерация мешей (Job System или Thread Pool)
- Бесшовные стыки между чанками
- Динамическая загрузка при движении игрока

**Результат:** Бесконечный плоский мир из чанков

### 1.4 Процедурная генерация
**Цель:** Красивый холмистый ландшафт с пещерами

**Технические требования:**
- Perlin/Simplex Noise для высоты (2D)
- Многооктавный шум для естественности
- 3D шум для пещер
- Seed для воспроизводимости
- Слои: трава → земля → камень → bedrock

**Результат:** Реалистичный ландшафт с холмами и пещерами

### 1.5 Стиль Cube World
**Цель:** Сглаженные воксели вместо острых кубов

**Технические требования:**
- Marching Cubes или Dual Contouring
- Сглаживание граней блоков
- Фаски и скругления
- Сохранение производительности
- Опциональная реализация (можно отложить)

**Результат:** Мир выглядит как в Cube World, не как в Minecraft

## Промпты для AI

### Пример 1: Базовый чанк
```
Напиши C# скрипт для Unity, который генерирует меш 
одного воксельного чанка 16x16x16. Каждый блок — куб. 
Генерируй только видимые грани (не рендери грани между 
соседними солидными блоками). Используй текстурный атлас.
```

### Пример 2: Greedy Meshing
```
Добавь Greedy Meshing к моему воксельному чанку. 
Объединяй соседние одинаковые грани в один большой полигон. 
Покажи до/после количество вершин.
```

## Технические детали

### Структура данных чанка
```csharp
public class Chunk
{
    public const int SIZE = 16;
    public byte[,,] blocks = new byte[SIZE, SIZE, SIZE];
    public Mesh mesh;
    public MeshFilter meshFilter;
    public MeshCollider meshCollider;
}
```

### Типы блоков
```csharp
public enum BlockType : byte
{
    Air = 0,
    Grass = 1,
    Dirt = 2,
    Stone = 3,
    Water = 4,
    Sand = 5,
    Snow = 6
}
```

## Метрики производительности

**Цели:**
- Генерация одного чанка: < 5ms
- FPS: 60+ при render distance 8 чанков
- Вершин на чанк: < 10000 (с Greedy Meshing)
- Memory per chunk: < 100KB

## Отладка

**Типичные проблемы:**
- Швы между чанками → проверить граничные грани
- Низкий FPS → профайлер, многопоточность
- Z-fighting → правильные нормали
- Неправильные UV → проверить атлас координаты